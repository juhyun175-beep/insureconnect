# v2.39.0: /sitemap.xml → /api/sitemap 302 규칙 제거.
#   /sitemap.xml 은 functions/sitemap.xml.js 가 직접 200으로 서빙(함수가 _redirects보다 우선).
#   기존 302 규칙은 함수에 가려 죽어 있었고, 함수 배포 이전 시점엔 Google 사이트맵 패치를
#   /api/sitemap 으로 302시켜 "가져올 수 없음"을 유발할 수 있던 랜드마인이라 제거함.
#   (현재 sitemap 단일 권위본 = /sitemap.xml = functions/sitemap.xml.js)

# v2.93.0: 렌트카·통신 완전 철수 — 기존 인바운드/북마크 링크는 홈으로 (SSR 페이지 삭제)
/rental    /  301
/telecom   /  301

# v2.105.0: fp-income-structure 옛 주소 301은 여기가 아니라 functions/insurance/[category]/[slug].js
#   (MOVED_SLUGS)에서 처리 — 함수 라우트가 _redirects보다 우선이라 여기 두면 죽은 규칙이 된다.
