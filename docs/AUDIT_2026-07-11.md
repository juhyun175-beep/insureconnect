# 전수 감사 보고서 — 2026-07-11 (v2.122.0)

> 범위: ① 깃허브/로컬/배포 동기화 ② 공고 유료옵션(추가 옵션) 승인 시 자동반영 구조 ③ 보안 취약점(관리자 포함)
> ④ 성능(버벅임) 원인 ⑤ 미사용 파일/죽은 코드 정리. 수정분은 CHANGELOG [2.122.0] 참고.

---

## 1. 동기화/배포 상태 — ✅ 정상

- 로컬 = `origin/main` = `8420122`(release v2.121.0) 동일.
- 라이브(insureconnect.co.kr) SSR 페이지에서 v2.121.0 마커(`menu:'홈광고'`, `popimp:`) 확인, 구버전 마커 없음 → **최신 커밋이 배포되어 있음**.
- 배포 파이프라인: main push → GitHub Actions(보안스캔 HIGH 0 게이트 → HTML 미니파이 → `wrangler pages deploy`). 이 PR을 main에 머지하면 자동 배포됨.

## 2. 유료옵션 자동반영 구조 — ✅ 전반적으로 잘 설계됨

승인(PATCH status=approved) 시 `fulfillApprovedOptions()`(functions/_lib/fulfillment.js)가 recruit/lecture/meetup 3종 모두에서 호출되고, 결과가 `ad_orders.fulfilled_json`에 기록되어 관리자 화면(주문 배지·승인 알럿)에 노출된다. 서버 가격 권위(클라는 key만 전달), 번들 전개, 재승인 멱등성(CASE 가드 + auto_done 스킵) 모두 확인.

| 옵션 | 승인 시 동작 | 노출 계층 | 판정 |
|---|---|---|---|
| 추천공고 등록 | `featured_until` +3일 보장 | 목록 API featured 우선 정렬 + 골드 배지 | 자동 ✅ |
| 1:1 문의 기능 | `dm_enabled=1` (비로그인 등록은 판매 차단) | 상세 버튼 + chat/dm 서버 게이트 | 자동 ✅ |
| 카카오톡 전 회원 알림 | 수신동의 회원 전원 즉시 발송 | — (발송형) | 자동 ✅ |
| SEO 상단 고정 7일+PICK | `seo_boost_until` +7일 | posting-widget 1번 슬롯 + PICK 배지(캐시 60s) | 자동 ✅ |
| 부스트 패키지 | SEO+오픈챗2회+카톡으로 전개 | 구성 옵션별 | 자동 ✅ (오픈챗 부분만 수동) |
| 홈 배너 7일 | home_ad 캠페인 자동 생성(시작~+6일) | /api/home-ad 활성 필터 + 홈 배너/팝업 | 자동 ✅ |
| 오픈채팅 게시/풀데이 | `manual_required` 기록 | 관리자 수동 배지 | 의도된 수동 ✅ |

### 발견사항(구조 개선 권고)

1. ~~**입금 게이트 없음**~~ → **v2.123.0 해소**: 관리자 `입금확인(mark_paid)` 액션(`pending_payment → paid` 전이) 추가. 카톡 발송은 입금대기 주문에서 보류되고 입금확인 시 자동 진행.
2. ~~**카톡 일괄발송 스케일 한계**~~ → **v2.123.0 해소**: `ic_kakao_queue` 대기열 + 청크(20명) 드레인으로 전환(cron 5분 + 대시보드 폴링 킥 + 등록 직후 1청크). 요청당 fetch가 상수로 고정되어 회원 수와 무관.
3. ~~**유료 추천공고 = 무료 맛보기 3일**~~ → **v2.123.0 해소**: 유료 추천공고는 잔여 노출기간에 **+7일 가산**(첫 승인이면 무료 3일 + 유료 7일 = 10일).
4. **환불 시 이행 롤백 없음**(잔존): 환불 승인해도 dm_enabled/홈배너 캠페인/featured가 남는다. 수기 환불 정책이라 치명적이진 않지만, 환불 시 체크리스트(배너 캠페인 비활성 등)라도 관리자 화면에 안내 권장.
5. **일간 브리핑/주간 다이제스트**(잔존): cron 발송이지만 여전히 요청 내 전량 발송 구조 — 수신자가 수백 명대로 늘면 카톡 대기열과 같은 방식으로 전환 필요(현재는 DRY 게이트 + 새벽 발송이라 여유 있음).

## 3. 보안 — 수정 6건 / 권고 4건

### 이번 PR에서 수정 (상세 CHANGELOG)
- **[높음] 공개 API 개인정보 노출**: 채용/강의 목록 GET이 등록자 이름·연락처를 비인증 반환, 단건 GET은 상태 무관 `SELECT *`(연락처·쿠폰 필드 포함) 공개 → 관리자 전용으로 게이트. 회귀 테스트 추가.
- **[중간] 업로드 남용**: user-upload 서버측 레이트리밋 없음 → IP당 20건/시간.
- **[중간] 오승인 비가역 발송**: 승인 확인창 입금대기 경고.
- **[낮음] 포인트 이중지불 race**, **[낮음] cron 시크릿 타이밍 비교**, **[정보] 죽은 Supabase anon key 하드코딩 3개 파일** 제거.

### 유지해도 되는 항목(권고)
- **관리자 인증 구조**: 정적 `x-admin-secret` + constant-time 비교 + sessionStorage 보관. 1인 운영엔 합리적. 단 verify 외 관리자 엔드포인트에는 실패 지연이 없으므로 **시크릿은 충분히 길게(24자+) 유지**하고 유출 의심 시 즉시 교체(`wrangler pages secret put ADMIN_SECRET`).
- **CORS `Access-Control-Allow-Origin: *`** 전 응답 공통: 헤더 기반 인증이라 실익 악용은 어려우나, 관리자 응답은 자체 도메인으로 좁히면 더 안전.
- **/api/track/*** 무인증 쓰기: 봇 필터 + 120일 자동정리 있음. D1 쓰기 남용 시 Turnstile 고려.
- **/api/files GET**: R2 버킷 전체가 공개 서빙 전제 — 버킷에 비공개 파일을 절대 넣지 말 것.
- 카카오 OAuth(state CSRF), 세션(해시 저장·httpOnly·Lax), DM 접근통제(서버 owner 도출), 관리자 회원 API(토큰 미노출·LIKE 이스케이프), SQL 파라미터 바인딩 전반: **양호**.

## 4. 성능(버벅임) 분석 — 구조 요인이 커서 로드맵 제안

측정(라이브): index.html **682KB**(미니파이, brotli 전송 152KB), admin.html **399KB**. index 인라인 JS ~392KB·CSS ~318KB, 정적 DOM ~2,700 노드.

원인 정리:
1. **단일 파일 아키텍처**: JS/CSS가 전부 HTML 인라인 → 브라우저가 **매 방문(캐시 60초)마다 682KB 재다운로드+재파싱**. 콘텐츠 1글자 변경에도 전체 무효화. 중급 모바일에서 첫 페인트 지연·입력 버벅임의 주원인.
2. **admin 10초 폴링**: 대시보드가 보이는 동안 10초마다 `/api/stats` + 재렌더(전체 innerHTML 갱신). 30~60초로 완화 권장.
3. 홈 60초×2 인터벌, 채팅 폴링(2.5s, 열려있을 때) 등은 탭 비활성 시 정지 처리가 이미 잘 되어 있음.

권장 로드맵(효과 순):
1. 인라인 JS/CSS를 `app.[hash].js`/`app.[hash].css`로 외부화 + 1년 immutable 캐시(_headers에 이미 규칙 존재) → 재방문 전송량 ~150KB→수 KB.
2. 등록 모달·뷰어·카드뉴스 등 거대 기능 블록 lazy 로드(현재도 32개 script 블록으로 나뉘어 있어 분리 용이).
3. admin 폴링 주기 완화 + 목록 diff 렌더.
4. (이번 PR로) 고아 이미지 3.4MB 제거 — 배포 아티팩트/저장소 경량화.

## 5. 파일/코드/DB 정리

### 이번 PR에서 삭제 (총 18개, ~3.5MB)
고아 이미지 14개(`connect.png` 1.6MB 포함 — 코드·D1 콘텐츠·블로그 초안 어디서도 미참조 확인), Supabase 전용 `cleanup.sql`, `.netlify/state.json`, `.wrangler/cache/*.json`(로컬 툴 상태) git 추적 제거.

### 유지 (판단 근거)
- `schema.sql`/`migrations/`: 스키마 이력. `docs/`, `GROWTH*.md`, `naver-blog/`, `seo-seed/`: 운영·콘텐츠 소스(모두 미들웨어+.assetsignore로 공개 차단됨).
- `baec816b...txt`: 도메인 소유 확인 파일. `.claude/launch.json`: 세션 실행 설정.

### D1 레거시 테이블 — **v2.123.0에서 가역 리네임 완료**
코드 참조 0건 확인된 7개(`ic_users`, `ic_sessions`, `ic_rental_inquiries`, `ic_rental_vehicles`, `ic_telecom_devices`, `ic_telecom_inquiries`, `ic_email_otps` — 총 14행)를 `zz_legacy_*` 접두사로 리네임(데이터 보존). 몇 주 문제 없으면 하드 DROP:
`npx wrangler d1 execute insureconnect-d1 --remote --command "DROP TABLE zz_legacy_ic_users"` 등.
`ic_visits_daily`는 deprecated(dead-write)로 GROWTH_NEXT에 이미 기록됨 — track/visit 비콘 정리 시 함께.

### 기타
- ~~**CI에 테스트 미실행**~~ → **v2.123.0 해소**: deploy.yml에 `node --test tests/*.test.js` 스텝 추가(17개).
- 레거시 공유링크 OG(`?news=`/`?recruit=`/`?post=`)는 v2.122.0(D1 전환)+v2.122.1(미니파이 마커 보존)로 **기능 복원 확인**(라이브 og:title 검증).
- 성능 로드맵 1단계(JS/CSS 외부화)는 **v2.123.0에서 배포 아티팩트 빌드 스텝으로 구현** — index.html 682KB→134KB(min), 자산 1년 캐시.
