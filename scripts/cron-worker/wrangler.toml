name = "insureconnect-cron"
main = "worker.js"
compatibility_date = "2024-11-01"
# cron 전용 — 공개 HTTP URL 불필요(스케줄만). 첫 워커 서브도메인 프롬프트 회피 + 비공개.
workers_dev = false

# 매일 07:00 KST(UTC 22:00) = 일간 브리핑 / 매주 월 08:00 KST(일 23:00 UTC) = 주간 다이제스트
[triggers]
crons = ["0 22 * * *", "0 23 * * 0"]
